@model IEnumerable<Biblioteka.Models.Category>
@{
    ViewBag.Title = "Kategorie";
}

<h2>Kategorie</h2>

<div>
    @foreach (var c in Model)
    {
        <div>
            @c.CategoryID @c.Name 
        </div>
    }
</div>
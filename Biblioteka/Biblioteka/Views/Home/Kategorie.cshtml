@model IEnumerable<Biblioteka.Models.Category>
@{
    ViewBag.Title = "Kategorie";
}

<h2>Kategorie</h2>

<div>
    @foreach (var c in Model)
    {
        <div>
            @c.id_category @c.name
        </div>
    }
</div>